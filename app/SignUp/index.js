import React from 'react';
import {style} from './index.scss';

const SignUp = ({}) => {

    return (
        <div className={style}>
            
        </div>
    );
};

SignUp.propTypes = {

};

export default SignUp;
